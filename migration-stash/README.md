# migration-stash

Desktop'tan PC değişimi öncesi alınan küçük dosya yedekleri (2026-05-07).

## İçerik

| Klasör | Açıklama |
|--------|----------|
| `Executables/` | `instagram_indir.bat`, `JARVIS.bat` — kısayol başlatıcılar |
| `el/` | `hand_control.py` — el (gesture) kontrol denemesi + log dosyaları |
| `youtube/` | (boş — yer tutucu) |
| `README/` | Eski Python denemeleri + `Enough-Reborn` (3rd party araç klonu) |
| `Obsidian.lnk` | Obsidian kısayolu (yeni PC'de yeniden oluşturulacak) |

## Hariç tutulanlar

- `Yeni klasör/` (Desktop, **2.5 GB**) — GitHub'a sığmaz, USB / Google Drive ile taşı
- `.venv/`, `__pycache__/` — Python build artifact'ları, gerekmiyor

## Not

`README/` altındaki bazı script'ler 3rd-party araçların klonu (`Enough-Reborn`), yedekleme amaçlı tutuluyor. Yeni PC'de gerek olmayanları sil.
