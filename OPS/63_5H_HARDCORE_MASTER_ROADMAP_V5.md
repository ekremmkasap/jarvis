# OPS/63_5H_HARDCORE_MASTER_ROADMAP_V5

Tarih: 2026-04-04
Mode: V5 hardcore roadmap
Intent: artifact-first, runtime-specific, adversarial, and interruption-proof.

## Global Contract
- This roadmap is not a hype document.
- Every segment has a tactical goal, owner, exact files, exact commands, evidence targets, contradiction, success criteria, fallback path, artifact updates, and handoff note.
- The immediate high-ROI trio stays visible: real --web-only, queue semantics, repo cleanup classification.
- Do not widen scope because a donor repo looks exciting.
- Subagent lane fabric is mostly simulated in this sprint; do not report fake spawned execution.
- No unsupported production-ready language.
- No fake green checks.
- No hiding contradictions.
- No secret exposure.
- No remote push, force-push, hard reset, or unrelated deletion.
- If interrupted, the next shift must be able to replay from OPS files without guessing.

## Lane Rules
### Lane 0 - Main Lead Orchestrator
- Rule: Own final truth, sequencing, and gate closure.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 1 - Evidence Miner
- Rule: Trace claim sources and downgrade unsupported certainty.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 2 - Runtime Cartographer
- Rule: Map canonical entrypoints and duplicate ownership.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 3 - Backend Stabilizer
- Rule: Apply the smallest correct code change and validate it immediately.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 4 - Failure Hunter
- Rule: Reproduce or bound failures without optimism.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 5 - OpenClaw and Model Integrator
- Rule: Keep profile/auth/operator truth conservative until re-proved.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 6 - Voice and Hologram Designer
- Rule: Protect interaction quality and no-fake-UX claims.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 7 - Memory and Self-Improvement Architect
- Rule: Separate real memory from aspirational memory.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 8 - Social and External Intelligence Agent
- Rule: Turn donor inventory into operator-grade intake contracts.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

### Lane 9 - Adversarial Verifier
- Rule: Attack weak evidence and kill fake green checks.
- Required output: evidence, confidence, unknowns, next consumer.
- Required update: OPS/64 plus at least one truth artifact.
- Abort condition: proof would widen blast radius beyond the lane goal.
- Anti-fake-green: never generalize a narrow pass to whole-system readiness.

## Artifact Update Rules
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

## Verification Discipline
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

## Twenty-Five Segment Execution Cards
### Segment 01: Baseline Prompt And Checkpoint Lock
- Phase: Hour 1
- Lead lane: Lane 1
- Support lane: Lane 9
- Executive gate: Chief of Staff + CTO
- Tactical goal: Lock V5 prompt count, inherited V2 checkpoint, and active truth hierarchy.
- Exact contradiction under attack: clean-slate mental model
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `OPS/jarvis.txt-v4v5.txt`
- `OPS/CODEX_5H_HARDCORE_PROMPT_V2.txt`
- `OPS/12_5H_HARDCORE_MASTER_ROADMAP.md`
- `OPS/13_EXECUTION_RUNLOG.md`
- `OPS/14_CLAIMS_VS_REALITY.md`

#### Exact Commands
- `(Get-Content "OPS\jarvis.txt-v4v5.txt").Count`
- `(Get-Content "OPS\CODEX_5H_HARDCORE_PROMPT_V2.txt").Count`
- `(Get-Content "OPS\12_5H_HARDCORE_MASTER_ROADMAP.md").Count`
- `git status --short`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- exact line counts
- prior checkpoint files
- dirty worktree proof
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 1.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 1.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 1.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 1.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 1.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 1.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 1.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 1.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 1.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 1.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 1.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 1.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 1
- Last checkpoint must mention Segment 01 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 02: Last 24h Forensic Sweep
- Phase: Hour 1
- Lead lane: Lane 1
- Support lane: Lane 0
- Executive gate: Chief of Staff
- Tactical goal: Separate done, partial, contradicted, and unverifiable work from the last 24 hours.
- Exact contradiction under attack: completion report equals runtime truth
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `OPS/10_LAST24_FORENSIC_AUDIT.md`
- `OPS/13_EXECUTION_RUNLOG.md`
- `OPS/17_STABILITY_REPAIRS.md`
- `OPS/19_NEXT_SHIFT_HANDOFF.md`

#### Exact Commands
- `Get-Content OPS\10_LAST24_FORENSIC_AUDIT.md -First 160`
- `Get-Content OPS\13_EXECUTION_RUNLOG.md -First 160`
- `Get-Content OPS\17_STABILITY_REPAIRS.md -First 200`
- `Get-Content OPS\19_NEXT_SHIFT_HANDOFF.md -First 200`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- prior forensic narrative
- repair notes
- handoff warnings
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 1.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 1.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 1.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 1.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 1.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 1.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 1.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 1.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 1.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 1.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 1.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 1.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 1
- Last checkpoint must mention Segment 02 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 03: Working Tree Stratification
- Phase: Hour 1
- Lead lane: Lane 9
- Support lane: Lane 3
- Executive gate: CEO + CFO
- Tactical goal: Classify the repo into committable, artifact-only, and ignore-only.
- Exact contradiction under attack: everything belongs in one commit
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `repo root`
- `OPS/`
- `apps/web-ui/.next/`
- `server/data/`
- `state/`

#### Exact Commands
- `git status --short`
- `Get-ChildItem OPS | Select-Object Name`
- `Get-ChildItem apps\web-ui\.next -Name`
- `Get-ChildItem server\data -Name`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- tracked modifications
- generated outputs
- scratch directories
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 9.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 9.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 9.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 9.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 9.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 9.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 9.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 9.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 9.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 9.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 9.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 9.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 9
- Last checkpoint must mention Segment 03 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 04: Local Subagent Reality Check
- Phase: Hour 1
- Lead lane: Lane 2
- Support lane: Lane 1
- Executive gate: COO
- Tactical goal: Audit local subagent availability and the difference between real execution and prompt generation.
- Exact contradiction under attack: shortcut exists therefore agents auto-run
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `.codex/agents/`
- `tools/subagents/README.md`
- `docs/SUBAGENT_MAPPING.md`
- `server/config/agent_manifests.json`

#### Exact Commands
- `Get-ChildItem .codex\agents | Select-Object Name`
- `Get-Content tools\subagents\README.md -First 80`
- `Get-Content docs\SUBAGENT_MAPPING.md -First 120`
- `Get-Content server\config\agent_manifests.json -First 120`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- local agent catalog
- prompt-generator note
- manifest mapping
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 2.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 2.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 2.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 2.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 2.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 2.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 2.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 2.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 2.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 2.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 2.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 2.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 2
- Last checkpoint must mention Segment 04 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 05: External Repo Intake Map
- Phase: Hour 1
- Lead lane: Lane 8
- Support lane: Lane 1
- Executive gate: CMO + CTO
- Tactical goal: Classify external repos into runtime candidate, donor, reference, or hold.
- Exact contradiction under attack: repo exists therefore merge-ready
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `external-repos/`
- `external-repos/Mark-XXXV/README.md`
- `external-repos/ClawRouter/README.md`
- `external-repos/OpenHands/README.md`
- `external-repos/youtube-mcp-server/README.md`

#### Exact Commands
- `Get-ChildItem external-repos | Select-Object Name`
- `Get-Content external-repos\Mark-XXXV\README.md -First 80`
- `Get-Content external-repos\ClawRouter\README.md -First 80`
- `Get-Content external-repos\OpenHands\README.md -First 80`
- `Get-Content external-repos\youtube-mcp-server\README.md -First 80`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- repo names
- README surfaces
- donor patterns
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 8.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 8.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 8.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 8.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 8.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 8.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 8.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 8.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 8.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 8.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 8.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 8.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 8
- Last checkpoint must mention Segment 05 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 06: Runtime Entrypoint Cartography
- Phase: Hour 2
- Lead lane: Lane 2
- Support lane: Lane 9
- Executive gate: CTO
- Tactical goal: Map canonical entrypoints for launcher, bridge, watchdog, orchestrator, wrappers, and voice surfaces.
- Exact contradiction under attack: docs naming equals canonical owner
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `master_launcher.py`
- `server/bridge.py`
- `server/watchdog.py`
- `services/orchestrator/main.py`
- `openclaw.cmd`
- `openclaw_web_only.cmd`

#### Exact Commands
- `rg "def main|if __name__|on_event|bridge" master_launcher.py server services openclaw*.cmd`
- `Get-Content openclaw.cmd`
- `Get-Content openclaw_web_only.cmd`
- `Get-Content install_openclaw_startup.cmd`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- entrypoint markers
- wrapper delegates
- health surface
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 2.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 2.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 2.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 2.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 2.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 2.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 2.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 2.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 2.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 2.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 2.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 2.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 2
- Last checkpoint must mention Segment 06 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 07: Bridge Watchdog Producer Contract
- Phase: Hour 2
- Lead lane: Lane 3
- Support lane: Lane 4
- Executive gate: CTO + COO
- Tactical goal: Re-check the bridge side of the watchdog heartbeat and lock contract.
- Exact contradiction under attack: file writer exists therefore restart semantics are solved
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/bridge.py`
- `server/watchdog.py`
- `server/data/`

#### Exact Commands
- `git diff -- server/bridge.py`
- `Get-Content server\watchdog.py -First 220`
- `python -m py_compile server\bridge.py server\watchdog.py`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- heartbeat file path
- lock file path
- cleanup path
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 3.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 3.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 3.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 3.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 3.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 3.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 3.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 3.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 3.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 3.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 3.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 3.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 3
- Last checkpoint must mention Segment 07 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 08: Real Web-Only Runtime Support
- Phase: Hour 2
- Lead lane: Lane 3
- Support lane: Lane 9
- Executive gate: CTO
- Tactical goal: Make sure web-only is a real runtime behavior and not just wrapper text.
- Exact contradiction under attack: web-only is only a label
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/runtime_config.py`
- `server/bridge.py`
- `openclaw_web_only.cmd`
- `install_openclaw_startup.cmd`
- `tests/test_runtime_config.py`

#### Exact Commands
- `git diff -- server/runtime_config.py`
- `git diff -- server/bridge.py`
- `Get-Content openclaw_web_only.cmd`
- `Get-Content install_openclaw_startup.cmd`
- `python -m unittest tests.test_runtime_config`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- CLI override function
- runtime label change
- telegram disable path
- wrapper invocation
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 3.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 3.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 3.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 3.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 3.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 3.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 3.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 3.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 3.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 3.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 3.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 3.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 3
- Last checkpoint must mention Segment 08 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 09: Queue Data Model Truth
- Phase: Hour 2
- Lead lane: Lane 3
- Support lane: Lane 4
- Executive gate: CTO
- Tactical goal: Clarify queue ordering, persistence, and confirmation semantics.
- Exact contradiction under attack: queue is only an in-memory FIFO
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `services/orchestrator/task_queue.py`
- `tests/test_task_queue.py`

#### Exact Commands
- `git diff -- services/orchestrator/task_queue.py tests/test_task_queue.py`
- `python -m unittest tests.test_task_queue`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- priority queue path
- state file path
- confirm path
- snapshot path
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 3.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 3.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 3.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 3.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 3.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 3.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 3.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 3.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 3.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 3.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 3.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 3.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 3
- Last checkpoint must mention Segment 09 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 10: Queue Health Semantics Cleanup
- Phase: Hour 2
- Lead lane: Lane 3
- Support lane: Lane 9
- Executive gate: CTO + COO
- Tactical goal: Expose queue truth through the health endpoint instead of only queue length.
- Exact contradiction under attack: queue_size alone is health
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `services/orchestrator/main.py`
- `services/orchestrator/task_queue.py`
- `tests/test_orchestrator_health.py`

#### Exact Commands
- `git diff -- services/orchestrator/main.py services/orchestrator/task_queue.py tests/test_orchestrator_health.py`
- `python -m unittest tests.test_orchestrator_health`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- queue_snapshot payload
- queued_by_priority
- awaiting_confirmation
- queue_state_file
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 3.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 3.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 3.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 3.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 3.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 3.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 3.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 3.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 3.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 3.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 3.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 3.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 3
- Last checkpoint must mention Segment 10 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 11: Orchestrator API Hardening
- Phase: Hour 2
- Lead lane: Lane 3
- Support lane: Lane 9
- Executive gate: CTO + CISO
- Tactical goal: Tighten invalid priority and invalid confirmation-state handling.
- Exact contradiction under attack: API is only happy path
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `services/orchestrator/main.py`
- `services/orchestrator/task_queue.py`

#### Exact Commands
- `git diff -- services/orchestrator/main.py services/orchestrator/task_queue.py`
- `python -m unittest tests.test_task_queue tests.test_orchestrator_health`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- 400 on invalid priority
- 409 on invalid confirm state
- queue.confirm path
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 3.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 3.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 3.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 3.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 3.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 3.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 3.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 3.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 3.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 3.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 3.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 3.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 3
- Last checkpoint must mention Segment 11 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 12: Wrapper And Startup Drift Audit
- Phase: Hour 2
- Lead lane: Lane 4
- Support lane: Lane 2
- Executive gate: COO + CISO
- Tactical goal: Align wrapper language, startup helper side effects, and current runtime truth.
- Exact contradiction under attack: legacy text still defines reality
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `openclaw.cmd`
- `openclaw_web_only.cmd`
- `install_openclaw_startup.cmd`
- `master_launcher.py`

#### Exact Commands
- `Get-Content openclaw.cmd`
- `Get-Content openclaw_web_only.cmd`
- `Get-Content install_openclaw_startup.cmd`
- `python -m py_compile master_launcher.py`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- wrapper text
- autostart write behavior
- launcher header
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 4.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 4.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 4.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 4.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 4.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 4.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 4.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 4.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 4.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 4.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 4.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 4.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 4
- Last checkpoint must mention Segment 12 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 13: Watchdog Live Smoke Design
- Phase: Hour 2
- Lead lane: Lane 4
- Support lane: Lane 0
- Executive gate: COO
- Tactical goal: Define the exact live smoke needed for watchdog restart semantics.
- Exact contradiction under attack: producer path means watchdog is done
- Segment class: core runtime truth
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/watchdog.py`
- `server/bridge.py`
- `OPS/17_STABILITY_REPAIRS.md`
- `OPS/19_NEXT_SHIFT_HANDOFF.md`

#### Exact Commands
- `Get-Content server\watchdog.py -First 220`
- `Get-Content OPS\17_STABILITY_REPAIRS.md -First 220`
- `Get-Content OPS\19_NEXT_SHIFT_HANDOFF.md -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- expected producer files
- prior repair notes
- remaining ROI note
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 4.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 4.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 4.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 4.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 4.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 4.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 4.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 4.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 4.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 4.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 4.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 4.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 4
- Last checkpoint must mention Segment 13 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 14: OpenClaw Profile Split Audit
- Phase: Hour 3
- Lead lane: Lane 5
- Support lane: Lane 1
- Executive gate: CTO + CISO
- Tactical goal: Map main, dev, or split profile truth without inventing runtime success.
- Exact contradiction under attack: profile split is already settled
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `openclaw.cmd`
- `openclaw_web_only.cmd`
- `server/bridge.py`
- `server/openclaw_bridge.py`
- `server/config/`

#### Exact Commands
- `rg "OpenClaw|telegram|model|profile|dev" server config docs -g "!external-repos/**"`
- `Get-Content openclaw.cmd`
- `Get-Content openclaw_web_only.cmd`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- profile mentions
- config sources
- bridge path
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 5.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 5.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 5.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 5.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 5.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 5.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 5.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 5.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 5.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 5.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 5.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 5.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 5
- Last checkpoint must mention Segment 14 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 15: Telegram Delivery Truth Matrix
- Phase: Hour 3
- Lead lane: Lane 5
- Support lane: Lane 9
- Executive gate: CPO + CISO
- Tactical goal: Keep direct send and agent-generated delivery claims conservative until re-proved.
- Exact contradiction under attack: it worked once therefore it works now
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/bridge.py`
- `server/telegram_webhook.py`
- `OPS/16_OPENCLAW_TELEGRAM_MODEL_STATE.md`
- `OPS/67_OPENCLAW_TELEGRAM_MODEL_STATE_V5.md`

#### Exact Commands
- `rg "Telegram|send_message|chat_id|authorized" server OPS -g "!external-repos/**"`
- `Get-Content OPS\16_OPENCLAW_TELEGRAM_MODEL_STATE.md -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- code paths
- older claims
- current lack of live send proof
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 5.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 5.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 5.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 5.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 5.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 5.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 5.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 5.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 5.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 5.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 5.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 5.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 5
- Last checkpoint must mention Segment 15 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 16: Model And Auth Failure Taxonomy
- Phase: Hour 3
- Lead lane: Lane 5
- Support lane: Lane 4
- Executive gate: CISO
- Tactical goal: Separate model alias, provider, auth, pairing, and routing failure classes.
- Exact contradiction under attack: all Telegram/OpenClaw failures are the same
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/bridge.py`
- `server/runtime_config.py`
- `server/openclaw_bridge.py`
- `config/`
- `docs/`

#### Exact Commands
- `rg "model|provider|auth|pair|allowlist|token" server config docs -g "!external-repos/**"`
- `Get-Content server\runtime_config.py -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- config keys
- validation rules
- missing proof list
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 5.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 5.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 5.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 5.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 5.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 5.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 5.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 5.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 5.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 5.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 5.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 5.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 5
- Last checkpoint must mention Segment 16 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 17: Voice Interruption Audit
- Phase: Hour 4
- Lead lane: Lane 6
- Support lane: Lane 9
- Executive gate: CPO
- Tactical goal: Treat early interruption as an open defect until behavior is measured and improved.
- Exact contradiction under attack: voice is acceptable already
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `services/voice/voice_service.py`
- `OPS/68_VOICE_HOLOGRAM_PC_MODE_DIRECTION_V5.md`
- `external-repos/Mark-XXXV/README.md`

#### Exact Commands
- `Get-Content services\voice\voice_service.py -First 260`
- `Get-Content external-repos\Mark-XXXV\README.md -First 80`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- current voice surface
- donor interruption-control patterns
- user target
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 6.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 6.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 6.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 6.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 6.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 6.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 6.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 6.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 6.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 6.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 6.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 6.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 6
- Last checkpoint must mention Segment 17 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 18: Hologram Direction Audit
- Phase: Hour 4
- Lead lane: Lane 6
- Support lane: Lane 2
- Executive gate: CPO + CTO
- Tactical goal: Map the current hologram/desktop embodiment surface and its gaps.
- Exact contradiction under attack: hologram lane is healthy or complete
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `apps/desktop-hologram/`
- `apps/web-ui/`
- `OPS/68_VOICE_HOLOGRAM_PC_MODE_DIRECTION_V5.md`

#### Exact Commands
- `Get-ChildItem apps\desktop-hologram`
- `Get-ChildItem apps\web-ui\src\app`
- `rg "hologram|voice|assistant" apps docs server -g "!external-repos/**"`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- local UI surfaces
- assistant-related terms
- missing donor note
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 6.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 6.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 6.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 6.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 6.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 6.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 6.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 6.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 6.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 6.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 6.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 6.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 6
- Last checkpoint must mention Segment 18 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 19: PC-Control Risk Gate
- Phase: Hour 4
- Lead lane: Lane 6
- Support lane: Lane 8
- Executive gate: CISO + COO
- Tactical goal: Define safety gates for PC-mode before any wider automation claims.
- Exact contradiction under attack: PC mode is a simple next feature
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `external-repos/Mark-XXXV/README.md`
- `tools/`
- `server/skills/run_command.py`
- `OPS/68_VOICE_HOLOGRAM_PC_MODE_DIRECTION_V5.md`

#### Exact Commands
- `Get-Content external-repos\Mark-XXXV\README.md -First 80`
- `rg "run_command|shell|execute" server tools -g "!external-repos/**"`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- PC-control donor claims
- local command-execution surfaces
- safety need
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 6.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 6.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 6.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 6.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 6.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 6.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 6.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 6.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 6.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 6.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 6.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 6.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 6
- Last checkpoint must mention Segment 19 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 20: Memory And Self-Heal Baseline Audit
- Phase: Hour 4
- Lead lane: Lane 7
- Support lane: Lane 9
- Executive gate: CTO + COO
- Tactical goal: Carry forward reliable baseline items honestly and note what was not freshly re-proved.
- Exact contradiction under attack: baseline reliable means no more audit
- Segment class: runtime-adjacent stabilization
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `server/skills/memory_skill.py`
- `server/agents/self_healer.py`
- `tests/test_memory_skill.py`
- `tests/test_self_healer.py`

#### Exact Commands
- `Get-Content server\skills\memory_skill.py -First 220`
- `Get-Content server\agents\self_healer.py -First 220`
- `Get-Content tests\test_memory_skill.py -First 220`
- `Get-Content tests\test_self_healer.py -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- current code presence
- baseline classification
- lack of fresh proof
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 7.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 7.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 7.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 7.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 7.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 7.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 7.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 7.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 7.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 7.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 7.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 7.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 7
- Last checkpoint must mention Segment 20 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 21: Self-Coding Memory Loop Design
- Phase: Hour 4
- Lead lane: Lane 7
- Support lane: Lane 3
- Executive gate: CTO
- Tactical goal: Define the minimum architecture for remembering changes, failures, fixes, and successful integrations.
- Exact contradiction under attack: self-improve already exists
- Segment class: controlled expansion and closeout
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `OPS/69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md`
- `server/skills/memory_skill.py`
- `OPS/64_EXECUTION_RUNLOG_V5.md`

#### Exact Commands
- `Get-Content OPS\69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md -First 220`
- `Get-Content server\skills\memory_skill.py -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- memory surface
- execution log artifact
- self-improvement requirements
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 7.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 7.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 7.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 7.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 7.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 7.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 7.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 7.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 7.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 7.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 7.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 7.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 7
- Last checkpoint must mention Segment 21 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 22: Social Intelligence Lane Definition
- Phase: Hour 5
- Lead lane: Lane 8
- Support lane: Lane 7
- Executive gate: CMO + CRO
- Tactical goal: Turn GitHub, YouTube, social-media, and AI-news intake into an operator-grade matrix.
- Exact contradiction under attack: social lane is already operational
- Segment class: controlled expansion and closeout
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `external-repos/youtube-mcp-server/README.md`
- `external-repos/mcp-server-youtube-transcript/README.md`
- `OPS/69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md`

#### Exact Commands
- `Get-Content external-repos\youtube-mcp-server\README.md -First 80`
- `Get-Content external-repos\mcp-server-youtube-transcript\README.md -First 80`
- `Get-Content OPS\69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- available local intake servers
- digest needs
- memory writeback path
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 8.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 8.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 8.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 8.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 8.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 8.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 8.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 8.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 8.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 8.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 8.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 8.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 8
- Last checkpoint must mention Segment 22 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 23: Daily 24h Digest Contract
- Phase: Hour 5
- Lead lane: Lane 8
- Support lane: Lane 0
- Executive gate: CMO + Chief of Staff
- Tactical goal: Define the daily digest as both summary and integration recommendation feed.
- Exact contradiction under attack: news summary alone is enough
- Segment class: controlled expansion and closeout
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `OPS/69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md`
- `OPS/70_SAAS_ASCENT_AND_NEXT_SHIFT_V5.md`

#### Exact Commands
- `Get-Content OPS\69_MEMORY_SELF_IMPROVEMENT_SOCIAL_AGENT_PLAN_V5.md -First 220`
- `Get-Content OPS\70_SAAS_ASCENT_AND_NEXT_SHIFT_V5.md -First 220`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- digest schema
- writeback requirements
- operator report rules
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 8.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 8.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 8.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 8.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 8.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 8.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 8.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 8.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 8.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 8.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 8.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 8.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 8
- Last checkpoint must mention Segment 23 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 24: SaaS Ascent Guardrails
- Phase: Hour 5
- Lead lane: Lane 8
- Support lane: Lane 9
- Executive gate: CEO + CFO + CRO
- Tactical goal: Describe a realistic SaaS path without overstating present maturity.
- Exact contradiction under attack: SaaS-ready claim without hardening
- Segment class: controlled expansion and closeout
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `external-repos/OpenHands/README.md`
- `OPS/70_SAAS_ASCENT_AND_NEXT_SHIFT_V5.md`
- `services/orchestrator/main.py`
- `services/orchestrator/task_queue.py`

#### Exact Commands
- `Get-Content external-repos\OpenHands\README.md -First 80`
- `Get-Content services\orchestrator\main.py -First 240`
- `Get-Content services\orchestrator\task_queue.py -First 260`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- CLI/local GUI/cloud inspiration
- current orchestrator baseline
- cleanup debt
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 8.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 8.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 8.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 8.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 8.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 8.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 8.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 8.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 8.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 8.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 8.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 8.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 8
- Last checkpoint must mention Segment 24 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

### Segment 25: Adversarial Closeout And Handoff
- Phase: Hour 5
- Lead lane: Lane 9
- Support lane: Lane 0
- Executive gate: Chief of Staff + COO
- Tactical goal: Close the sprint with truth set, remaining risks, and exact next actions.
- Exact contradiction under attack: a summary exists therefore the sprint is closed
- Segment class: controlled expansion and closeout
- Status rule: segment is open until evidence and unknowns are both written.

#### Exact Files
- `OPS/64_EXECUTION_RUNLOG_V5.md`
- `OPS/65_CLAIMS_VS_REALITY_V5.md`
- `OPS/66_RUNTIME_CANON_V5.md`
- `OPS/70_SAAS_ASCENT_AND_NEXT_SHIFT_V5.md`

#### Exact Commands
- `Get-Content OPS\64_EXECUTION_RUNLOG_V5.md -First 240`
- `Get-Content OPS\65_CLAIMS_VS_REALITY_V5.md -First 240`
- `Get-Content OPS\66_RUNTIME_CANON_V5.md -First 240`
- `Get-Content OPS\70_SAAS_ASCENT_AND_NEXT_SHIFT_V5.md -First 240`
- If a command is broader than the contradiction, narrow it.
- If a command would mutate risky state without proof value, defer it.

#### Exact Evidence Targets
- runlog
- claims matrix
- runtime canon
- next shift rules
- Confidence note after evidence review: high, medium, or low.
- Missing evidence must be named explicitly.

#### Success Criteria
- The contradiction is reduced, confirmed, or escalated with proof.
- At least one OPS truth artifact is updated.
- Unknowns are listed explicitly.
- No claim exceeds the evidence strength.
- The next lane can continue without reconstructing context from scratch.

#### Fallback Path
- If live runtime proof is unavailable, fall back to current code and mark the result partial.
- If execution risk is too high, write the smoke design and gate requirements.
- If the worktree is too dirty, isolate only the files relevant to this segment.
- If donor scope is too wide, harvest only the exact pattern needed.
- If proof contradicts older OPS docs, update claims instead of hiding the conflict.

#### Validation Rules
- Use the smallest correct proof first.
- If proof is only code-level, label it code-level.
- If proof is only test-level, label it narrow proof.
- If proof is absent, status cannot exceed PARTIAL or UNVERIFIED.
- If code and docs disagree, docs are stale.
- If wrapper and runtime disagree, wrapper is misleading.
- If live runtime is missing, do not say production-ready.
- If a contradiction remains, keep it visible in OPS/65.

#### Failure Branches
- Command not available -> switch to nearest read-only proof and record the gap.
- Test fails -> capture exact failing surface and stop claiming green.
- File missing -> mark missing, not optional.
- Repo too dirty -> isolate the relevant subset before any staging talk.
- Donor repo too large -> harvest only the pattern, not the runtime.
- Runtime smoke too risky -> write the smoke design and the gate instead.
- Older OPS docs conflict -> update claims and canon, do not hide the conflict.
- Time pressure -> leave one exact next command, not a broad suggestion.

#### Artifact Updates
- Append the exact command and result to OPS/64.
- Update OPS/65 if any claim moved.
- Update OPS/66 if ownership or canonical path moved.
- Update OPS/67 only if OpenClaw or Telegram truth moved.
- Update OPS/68 only if voice, hologram, or PC-mode truth moved.
- Update OPS/69 only if memory, self-heal, self-coding, or social lane truth moved.
- Update OPS/70 if next actions or cleanup classification moved.
- If interrupted, leave the last safe checkpoint and next exact command.

#### Tactical Execution Steps
- Step 1: Capture pre-state in one sentence and one proof command.
- Step 1 output: a short note tied to Lane 9.
- Step 1 abort condition: if this step would inflate status, stop and downgrade.
- Step 2: Read only the exact files that define the active behavior.
- Step 2 output: a short note tied to Lane 9.
- Step 2 abort condition: if this step would inflate status, stop and downgrade.
- Step 3: Separate inherited claim from current proof.
- Step 3 output: a short note tied to Lane 9.
- Step 3 abort condition: if this step would inflate status, stop and downgrade.
- Step 4: Write the strongest support for the claim.
- Step 4 output: a short note tied to Lane 9.
- Step 4 abort condition: if this step would inflate status, stop and downgrade.
- Step 5: Write the strongest contradiction for the claim.
- Step 5 output: a short note tied to Lane 9.
- Step 5 abort condition: if this step would inflate status, stop and downgrade.
- Step 6: Decide whether code change is justified right now.
- Step 6 output: a short note tied to Lane 9.
- Step 6 abort condition: if this step would inflate status, stop and downgrade.
- Step 7: If changing code, make the smallest coherent edit only.
- Step 7 output: a short note tied to Lane 9.
- Step 7 abort condition: if this step would inflate status, stop and downgrade.
- Step 8: Run the smallest validation that can falsify the change.
- Step 8 output: a short note tied to Lane 9.
- Step 8 abort condition: if this step would inflate status, stop and downgrade.
- Step 9: Update the runlog immediately after the proof.
- Step 9 output: a short note tied to Lane 9.
- Step 9 abort condition: if this step would inflate status, stop and downgrade.
- Step 10: Update the claims matrix with current confidence.
- Step 10 output: a short note tied to Lane 9.
- Step 10 abort condition: if this step would inflate status, stop and downgrade.
- Step 11: Update runtime canon or direction docs if ownership moved.
- Step 11 output: a short note tied to Lane 9.
- Step 11 abort condition: if this step would inflate status, stop and downgrade.
- Step 12: Leave a next-lane handoff note with one exact next command.
- Step 12 output: a short note tied to Lane 9.
- Step 12 abort condition: if this step would inflate status, stop and downgrade.

#### Handoff If Interrupted
- Current owner: Lane 9
- Last checkpoint must mention Segment 25 by name.
- Leave the last successful command verbatim.
- Leave the next exact command or file read.
- Leave the top unresolved contradiction in one sentence.
- Leave confidence with one short reason.

#### Do Not Claim
- Do not call the segment done if the contradiction merely moved elsewhere.
- Do not cite broad system health from a narrow test pass.
- Do not treat a wrapper name as runtime proof.
- Do not treat old reports as fresh proof.
- Do not expand into a new lane before this contradiction is closed or explicitly deferred.

## Appendix A: Immediate High-ROI Trio
- Real --web-only support in server/bridge.py
- Queue health and queue semantics cleanup
- Repo cleanup classification: committable / artifact-only / ignore-only
- These stay visible in runlog, claims, runtime canon, and next-shift docs.

## Appendix B: Command Bundles
### Prompt baseline
- `(Get-Content "OPS\jarvis.txt-v4v5.txt").Count`
- `(Get-Content "OPS\CODEX_5H_HARDCORE_PROMPT_V2.txt").Count`
- `(Get-Content "OPS\12_5H_HARDCORE_MASTER_ROADMAP.md").Count`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

### Bridge runtime
- `git diff -- server/runtime_config.py`
- `git diff -- server/bridge.py`
- `python -m unittest tests.test_runtime_config`
- `python -m py_compile server\runtime_config.py server\bridge.py`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

### Queue semantics
- `git diff -- services/orchestrator/task_queue.py services/orchestrator/main.py tests/test_task_queue.py tests/test_orchestrator_health.py`
- `python -m unittest tests.test_task_queue tests.test_orchestrator_health`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

### Wrapper truth
- `Get-Content openclaw.cmd`
- `Get-Content openclaw_web_only.cmd`
- `Get-Content install_openclaw_startup.cmd`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

### Subagent reality
- `Get-ChildItem .codex\agents | Select-Object Name`
- `Get-Content tools\subagents\README.md -First 80`
- `Get-Content docs\SUBAGENT_MAPPING.md -First 120`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

### Source intake
- `Get-ChildItem external-repos | Select-Object Name`
- `Get-Content external-repos\Mark-XXXV\README.md -First 80`
- `Get-Content external-repos\OpenHands\README.md -First 80`
- `Get-Content external-repos\youtube-mcp-server\README.md -First 80`
- Use only the subset needed for the active contradiction.
- Record the output in OPS/64 immediately after running.

