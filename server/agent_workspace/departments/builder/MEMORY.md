# Builder Memory

- For auth flows, require password hashing and generic error handling.
- For runtime work, keep `server/bridge.py` as the active entrypoint unless migration is explicit.
- Prefer stable local models when speed matters.
